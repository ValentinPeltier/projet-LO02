/**
 * Provides controllers classes in the MVC architecture for the application.
 * Controllers add action listeners to the views so that a controller method is called when an action is performed.
 * Then controllers call models methods according to the action performed to update the application data.
 */

package fr.utt.lo02.tdvp.controller;
